
#cloud-config
hostname: kworker-1
fqdn: kworker-1.local
manage_etc_hosts: true
users:
  - name: alice
    shell: /bin/bash
    sudo: ["ALL=(ALL) NOPASSWD:ALL"]  # Optional: grant passwordless sudo
    groups: users
    ssh-authorized-keys: []  # No SSH keys
    lock_passwd: false  # Allow password-based login

ssh_pwauth: true  # Enable SSH password authentication
disable_root: false  # Allow root login (set to true to disable)

chpasswd:
  list: |
    alice:password123
  expire: false

# Optional: ensure SSH daemon allows password authentication
runcmd:
  - sed -i '/^127\.0\.0\.1 localhost$/a 127.0.1.1 ubuntu' /etc/hosts












  # - sudo parted /dev/vdb --script mklabel gpt # initialize the disk with parted
  # - sudo parted /dev/vdb --script mkpart primary ext4 0% 100% # create a partition with parted, which is non interactive unlike fdisk
  # - sudo mkdir /mnt/mydisk # create mounting point folder
  # - yes y | sudo mkfs.ext4 /dev/vdb # apply a filesystem to the whole disk, wichh is wrong, but don't know the name of the partition yet
  # - sudo mount /dev/vdb /mnt/mydisk # mount the partition to the previously created folder














# Move /usr to a bigger partition
#  - sudo rsync -aPX /usr/ /mnt/mydisk/usr/ # Pick a large partition (e.g., /mnt/mydisk) and copy /usr there
#  - sudo mv /usr /usr.old # Rename old /usr
#  - sudo mount --bind /mnt/mydisk/usr /usr # Mount the new /usr
#  - /mnt/mydisk/usr  /usr  none  bind  0 0 # If it works, add it to /etc/fstab
# This gives /usr/bin effectively all the space of your larger partition.
# ✅ After this, sudo apt install docker.io will succeed, because binaries go into /usr/bin.





































  - sed -i 's/^#\?PasswordAuthentication .*/PasswordAuthentication yes/' /etc/ssh/sshd_config
  - sed -i 's/^#\?PermitRootLogin .*/PermitRootLogin yes/' /etc/ssh/sshd_config
  - systemctl restart sshd



















  - sudo bash -c 'echo "nameserver 8.8.8.8" > /etc/resolv.conf' # temporarily use Google's DNS

  - sudo apt update
  - sudo apt install docker.io -y
  - docker --version
  - systemctl status docker









  - sudo bash -c 'echo "nameserver 8.8.8.8" > /etc/resolv.conf' # temporarily use Google's DNS

  # Update and install dependencies

  - sudo apt update
  - sudo apt install -y apt-transport-https ca-certificates curl gpg

  # Add the Kubernetes GPG key

  - sudo mkdir -p /etc/apt/keyrings
  - curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.30/deb/Release.key | sudo gpg --dearmor -o /etc/apt/keyrings/kubernetes-apt-keyring.gpg

  # Add the Kubernetes APT repository

  - echo "deb [signed-by=/etc/apt/keyrings/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.30/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list

  # Update package lists

  - sudo apt update

  # Install kubeadm (and optionally and kubelet)

  - sudo apt install -y kubeadm kubelet kubernetes-cni

  # Prevent automatic updates (optional but recommended)

  - sudo apt-mark hold kubeadm kubelet
